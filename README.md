# AntD + Tailwind List Demo

## Run
```bash
npm install
npm run dev
```

Open the URL shown (usually http://localhost:5173).

This demo uses Ant Design `List` + Tailwind `group-hover` transitions with dummy data.
